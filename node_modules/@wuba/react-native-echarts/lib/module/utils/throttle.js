export function throttle(func, wait) {
  let lastExecution = 0;
  return (...args) => {
    const now = Date.now();
    if (now - lastExecution >= wait) {
      lastExecution = now;
      func(...args);
    }
  };
}
//# sourceMappingURL=throttle.js.map